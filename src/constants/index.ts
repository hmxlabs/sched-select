export const APP_CONFIG = {
  TITLE: "Scheduler Selector",
  DESCRIPTION: "Find the best scheduler for your needs",
  VERSION: "1.0.0"
} as const;

export const ROUTES = {
  HOME: "/",
} as const;

export const ANIMATION_DURATION = {
  FAST: 0.3,
  NORMAL: 0.5,
  SLOW: 0.8,
} as const;
